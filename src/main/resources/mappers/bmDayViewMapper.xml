<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="biz.model.BmDayViewDao">
	<select id="selectListAll" resultType="BmDayView">
		select * from bmDayView
	</select>
	<select id="selectListByYear" parameterType="int" resultType="BmDayView">
		select * from bmDayView where year &gt;= #{param1} and month &gt;=#{param2} and nowDay &gt;= #{param3} 
		and year &lt;= #{param4} and month &gt;=#{param5} and nowDay &lt;=#{param6}		
	</select>
</mapper>