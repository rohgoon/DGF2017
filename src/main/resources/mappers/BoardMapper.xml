<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board.model.BoardDao"> 
    
    <resultMap id="BoardResult" type="Board">
        <id property="boardNo" column="board_no"/>
        <result property="boardName" column="board_name"/>        
    </resultMap>
    
	<insert id="createBoard" parameterType="string">
      INSERT INTO dgf.board(board_name) VALUES(#{board_name})
	</insert>
	<select id="selectAllBoardList" resultType="Board">
	    select board_no, board_name from board order by board_no 
	</select>

</mapper>